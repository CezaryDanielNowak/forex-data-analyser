import React from 'react';
import BasePage from 'pages/BasePage';

export default class AdminAuthentications extends BasePage {
  className = 'ts-AdminAuthentications';

  render() {
    return (
      <div>
        Restricted admin area
      </div>
    );
  }
}
